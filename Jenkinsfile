pipeline {
    
    agent { label 'deploy-webapp' } 
    
    stages {
        stage('build') {
            steps {
                ls
                echo 'build'
                
                }
            }
        }
}
